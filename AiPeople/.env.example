# 复制为 .env 后填入。.env 不要提交到版本库。
# 用于人生语料生成 + 一致性 LLM 裁判（M0）以及后续的抽取器/反射器。

# OpenAI 兼容 endpoint。默认指向官方；换供应商见 README「接入模型」。
OPENAI_BASE_URL=https://api.openai.com/v1
# GLM 示例:        https://open.bigmodel.cn/api/paas/v4
# DeepSeek 示例:   https://api.deepseek.com
# Qwen 示例:       https://dashscope.aliyuncs.com/compatible-mode/v1
# 本地 Ollama:     http://localhost:11434/v1

OPENAI_API_KEY=sk-xxxxxxxx

# 强模型（生成语料 + 裁判），默认值见 config.yaml，可用环境变量覆盖：
STRONG_MODEL=gpt-4o-mini

# 迁移后历史模型和训练输出的外部根目录。
AIPEOPLE_MODEL_ROOT=F:\AiPeople
