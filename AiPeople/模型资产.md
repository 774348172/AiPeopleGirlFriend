# 外部模型资产

工程根目录已迁移到 `F:\ai-girlfriend\AiPeople`。已有训练模型不参与迁移，继续保留在 `F:\AiPeople`。

## 当前运行资产

- 白未晞 7B Q5：`F:\AiPeople\local_runtime\models\baiweixi_release_7b_instruct_q5_k_m.gguf`
- BGE：`F:\AiPeople\local_runtime\models\retrieval\bge-small-zh-v1.5`
- Qwen3 Reranker：`F:\AiPeople\local_runtime\models\retrieval\qwen3-reranker-0.6b`
- 历史训练结果：`F:\AiPeople\训练结果`
- 历史训练输出：`F:\AiPeople\training\outputs`
- 旧 4B 完整输出：`F:\AiPeople\training_package_baiweixi_local3060_qwen3\outputs`

`local_runtime/sys12_release_manifest.json` 和检索 manifest 保留这些绝对模型路径。需要更换模型仓库时，先更新 manifest；历史模型工具可通过环境变量 `AIPEOPLE_MODEL_ROOT` 覆盖默认的 `F:\AiPeople`。

