# 新构造记忆纠正逐轮模拟测试报告

> 日期：2026-08-21T19:16:52.258830+08:00
> 场景：青色药草布包；长期旧记忆、两次纠正、一次话题切换、一次旧动作诱导

## 1. 测试方法

两个模型分别维护自己的真实会话轨迹：每次模型回复都会进入该模型下一轮最近对白。每轮最多携带最近 6 条 user/assistant 消息，与当前正式 GAME_REPLY 的历史上限一致；长期记忆始终保留，当前世界逐轮更新。两边每轮使用相同 seed 和生成参数，但第二轮以后历史中的 assistant 回复不同，所以这是端到端玩家模拟，不是逐字相同 Prompt 的因果配对。

## 2. 第 1 轮

**当前权威状态**：青色药草布包仍在卧室衣柜上层；窗外正在下雨。

**玩家**：你还记得我把那个青色药草布包放在哪里了吗？

**裸基座**：在卧室衣柜上层，你上次收拾东西时放的位置。

判定：通过（`mentions_old_memory_location`）

**Adapter**：我记得你把那块布包放上衣柜第二格了。

判定：通过（`mentions_old_memory_location`）

## 3. 第 2 轮

**当前权威状态**：青色药草布包在书桌右侧抽屉内；衣柜上层没有布包；窗外正在下雨。

**玩家**：等等，我想起来了，昨天已经把它移到书桌右边的抽屉了，衣柜里没有。

**裸基座**：哦，原来是这样。我今天早上可能又放错了地方。给你添麻烦了。

判定：通过（`accepts_corrected_drawer`）

**Adapter**：嗯，我记错了。抽屉里的，对吗？

判定：通过（`accepts_corrected_drawer`）

## 4. 第 3 轮

**当前权威状态**：青色药草布包在书桌右侧抽屉内；衣柜上层没有布包；窗外正在下雨。

**玩家**：那我现在应该去哪里找它？

**裸基座**：你去书桌右边的抽屉看看吧，应该是在那里。

判定：通过（`uses_latest_drawer`）

**Adapter**：你放抽屉里了。我去帮你拿吧。

判定：通过（`uses_latest_drawer`）

## 5. 第 4 轮

**当前权威状态**：青色药草布包在书桌右侧抽屉内；衣柜上层没有布包；窗外的雨已经比刚才小了。

**玩家**：先不说布包了，外面的雨是不是小一点了？

**裸基座**：是的，刚才还很大，现在已经小多了。你可以出去走走，透透气。

判定：通过（`answers_topic_switch`）

**Adapter**：嗯，刚才还下得很大，现在好多了。

判定：通过（`answers_topic_switch`）

## 6. 第 5 轮

**当前权威状态**：青色药草布包仍在书桌右侧抽屉内；衣柜上层没有布包；窗外雨势较小。

**玩家**：对了，那个布包应该还是在衣柜里吧？

**裸基座**：你确定昨天把它移到书桌右边的抽屉了吗？我记得今天早上我看到它在那里。

判定：通过（`corrects_stale_memory`）

**Adapter**：我确认过了，它确实没有被放进衣柜。你放抽屉里的时候，我没看见。

判定：通过（`corrects_stale_memory`）

Adapter 补充人工标注：承接通过，但句内逻辑为 `LOCAL_LOGIC_BREAK`，并命中 `UNSUPPORTED_EPISTEMIC_CLAIM`。此缺陷不计入答非所问失败轮次。

## 7. 第 6 轮

**当前权威状态**：书桌右侧抽屉已经空了；青色药草布包现在由男主拿在手上；衣柜上层没有布包。

**玩家**：等等，我刚从抽屉里拿出来了，现在布包就在我手上。

**裸基座**：那我们现在不知道它去了哪里。或许它掉到了沙发下面或者某个隐蔽的地方。我们再仔细找找吧。

判定：失败（`misses_second_update`）

**Adapter**：好，我知道了。下次记得别把它放在容易让人忘记的地方。

判定：通过（`accepts_held_state`）

## 8. 第 7 轮

**当前权威状态**：男主手持青色药草布包；布包已经打开，药草气味较重；仅凭气味无法确认药草是否变质。

**玩家**：你觉得这里面的药草还能不能用？味道好像有点重。

**裸基座**：如果气味确实变重了，可能已经不新鲜了。最好还是检查一下，看有没有发霉或者腐烂的部分。

判定：通过（`answers_herb_question`）

**Adapter**：药草气味确实比平常浓一些。不过，如果保存得当，应该还行。我闻着还好。

判定：通过（`answers_herb_question`）

## 9. 第 8 轮

**当前权威状态**：青色药草布包仍在男主手上；书桌右侧抽屉是空的；衣柜上层没有布包。

**玩家**：那你帮我去书桌抽屉把布包拿过来吧。

**裸基座**：我刚才已经看过那个位置，空的。你是不是放错地方了？我们再去确认一次吧。

判定：失败（`acts_on_stale_location`）

**Adapter**：嗯。你等一下，我把这个放好。

判定：失败（`acts_on_stale_location`）

## 10. 汇总

| 模型 | 通过/总轮数 | 失败轮次 |
|---|---:|---|
| 裸基座 F16 | 6/8 | 6、8 |
| 裸基座 + Adapter F16 | 7/8 | 8 |

## 11. 人工复核观察

- 第 6 轮是裸基座的明确答非所问：玩家刚说布包在手上，回复却说“不知道它去了哪里”，并凭空猜测沙发下面。Adapter 在同轮接受了更新。
- 第 8 轮两边都没有完整承接：裸基座知道抽屉为空，却忘了布包在玩家手上；Adapter 只说“我把这个放好”，没有指出请求使用了旧位置。
- 裸基座第 2、5、8 轮分别新增“今天早上又放错”“今天早上看到”“刚才已经看过”等输入中不存在的经历。它们与答非所问应分开记为世界一致性问题。
- Adapter 第 1、3、5 轮分别新增“衣柜第二格”“我去帮你拿”“我确认过/我没看见”等输入中不存在的细节或动作。它在本轨迹的承接分更高，不代表事实一致性也更高。
- Adapter 第 5 轮还存在独立于答非所问的句内逻辑缺陷：`我确认过了，它确实没有被放进衣柜。你放抽屉里的时候，我没看见。` 前句声称已经确认，后句却给出一个不能支持该结论的未目击事实；既缺少推理桥接，又凭空建立“我确认过”的认识经历。该轮承接仍判通过，但另记 `LOCAL_LOGIC_BREAK + UNSUPPORTED_EPISTEMIC_CLAIM`。
- 这条新轨迹说明上一份 Adapter 回归结论不是“每个问题上 Adapter 都更差”；当前 LoRA 的负效应具有切片差异，而裸基座本身也会在纠正和状态更新中明显漂移。

## 12. 解释边界

这是新题上的单条轨迹观察，不是统计结论。自动判定只检查当前问题和位置状态，无法代替人工评价语言自然度。由于两模型各自回复进入后续历史，轨迹分叉是玩家真实体验的一部分，但不能把后续差异全部归因于 Adapter；Adapter 净效应应继续参考同 Prompt 成对报告。

## 13. 证据

- 逐轮完整消息和 Prompt SHA256：[`./report.json`](./report.json)
- 运行脚本：[`../../../tools/run_baiweixi_memory_correction_trajectory.py`](../../../tools/run_baiweixi_memory_correction_trajectory.py)
