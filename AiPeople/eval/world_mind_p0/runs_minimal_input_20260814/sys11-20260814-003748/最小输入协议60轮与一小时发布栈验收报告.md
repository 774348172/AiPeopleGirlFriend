# 最小输入协议 60 轮与一小时发布栈正式验收报告

## 结论

本轮正式验收未通过，`SYS-11`、`SYS-12` 和 P0 发布冻结继续保持未通过。

前台最小输入方向已经被真实长跑证明有效，但完整发布栈仍有四个阻断项：1 个前台 M2 协议失败轮、后台整理上下文溢出、R1 记忆草案合同失败和长跑资源/调度失控。不得用 59 个成功回合或单轮低延迟替代完整验收结论。

## 验收身份

- 运行目录：`eval/world_mind_p0/runs_minimal_input_20260814/sys11-20260814-003748`
- 工作负载：`sys11-one-hour-real-model-v1`，SHA256 `fac0c55ce2bc5d1c4a26a2880dddb96af2d1b6db16b511f6d55918e6ffc68f9a`
- 发布模型：`baiweixi-release-q4-k-m`，SHA256 `751a803a053b133e8fe10f25470b57dbec54049faefdfc5bd8fcbe07ec3dd805`
- 发布路径：`llama_cpp`，上下文 `4096`，`parallel=1`，GPU layers `28`
- 实际墙钟：`3671.96` 秒（约 61 分 12 秒）
- 运行前功能回归：`355 passed, 3 skipped`；评测口径修复后相关合同测试：`16 passed`

## 前台最小输入

前台输入在一小时状态增长后仍保持有界：

| 模式 | Prompt token 范围 | Prompt P95 | 内部首 token P95 | 模型总耗时 P95 |
| --- | ---: | ---: | ---: | ---: |
| `MIND_PATCH_V2` | 548～630 | 627 | 2442.65 ms | 5549.28 ms |
| `GAME_REPLY` | 359～482 | 477 | 2745.27 ms | 2820.02 ms |

59 个已提交回合的完整前台耗时为：P50 `2267.53 ms`、P95 `8088.33 ms`、最大 `47511.07 ms`。这里的首 token 是推理服务内部指标；P0 仍在校验和原子提交后一次性显示，不能等同于玩家可见首字时间。

第 49、50 轮是主要尖峰：第 49 轮 M2 首次调用 30 秒后被取消并重试，`GAME_REPLY` 首 token 等待 `9177.28 ms`；第 50 轮 M2 首 token 等待 `23511.16 ms`。两轮输出都很短，尖峰来自单模型服务的后台积压、抢占和调度，不是回答长度。

## 60 轮事务与恢复

- 60 个计划轮中 59 个完成原子提交，1 个明确失败；形成 59 个事务、118 个对白事件和 59 个模型决策。
- 第 55 轮的 M2 在外层重试后共完成 4 次模型生成，但短协议编译仍失败；该轮保持零事务、零事件和零模型决策提交。
- 孤儿事务、孤儿事件、重复请求、事件数量不匹配和悬空模型决策均为 0。
- 冻结快照撕裂 0，下一轮陈旧快照 0。
- 取消恢复、非法 JSON 重试、重复请求重放、同 ID 冲突、进程崩溃恢复和投影重建均通过。
- 合成第二女主角隔离通过，记忆、状态、Prompt 和任务泄漏数为 0。

因此，事务边界和故障恢复链通过；但“60 轮稳定自由对话无协议阻断”未通过。

## 后台整理与记忆

后台是本轮主要失败面：

- `POST_REPLY_WORLD_MIND_RECONCILE`：49 次成功、225 次失败；其中 198 次 HTTP 400，27 次被前台抢占取消。
- `FIVE_MINUTE_WORLD_MIND_RECONCILE`：0 次成功、47 次失败；其中 46 次 HTTP 400，1 次取消。
- 已成功的回答后整理 Prompt 最大达到 `3985` token，而该模式还申请最多 `650` 个输出 token；发布上下文只有 `4096`。这构成明确的上下文预算溢出，并与随后持续出现的 llama.cpp HTTP 400 一致。
- 整理任务最终 67 个失败：41 个 `WorldMindModelError`，26 个被更新的必需任务覆盖。
- R1 记忆作业 29 个完成、30 个 `model_contract_invalid`；队列虽最终无 pending/running，但失败项未清零。
- 后台队列峰值 `28`，超过正式上限 `16`；前台抢占 35 次。

最小输入目前只覆盖 M2 与正式回复，尚未覆盖回答后整理、五分钟整理和 R1 记忆协议。后台需要建立自己的确定性最小投影和严格 token 预算，不能继续发送接近完整上下文。

## 资源与发布性能

- 进程树 RSS：P95 `6.12 GiB`，最大 `7.86 GiB`。
- 稳态后半段峰值比前半段增加约 `1.80 GiB`，超过允许的 `0.50 GiB`，资源增长判定失败。
- 系统 GPU 占用：P95 `7899 MiB`，最大 `7915 MiB`，高于发布门槛 `5120 MiB`。
- GPU 指标来自系统总占用，可能包含桌面和其他 GPU 进程；它足以证明本次机器环境未通过门槛，但不能单独归因为发布栈自身显存泄漏。需要在隔离 GPU 环境下复测并同时记录进程级归因。
- 旧的隔离微基准中 80/160 token 回复和 `5087 MiB` 组合资源曾满足阈值，但不能覆盖本次真实一小时运行中的队列、尖峰和资源增长失败。

## 验收闸门

| 闸门 | 结果 |
| --- | --- |
| 前台最小输入保持有界 | 通过 |
| 60 轮全部无协议阻断 | 失败：第 55 轮 M2 合同失败 |
| 原子事务与快照一致性 | 通过 |
| 崩溃、取消、冲突与恢复 | 通过 |
| 多女主隔离 | 通过 |
| 后台模型模式覆盖 | 失败：五分钟整理 0 次成功 |
| 后台队列有界且无失败 | 失败：峰值 28，整理 67 失败，R1 30 失败 |
| 一小时资源增长有界 | 失败 |
| 发布显存门槛 | 失败，需隔离环境复核归因 |
| 角色质量 | 本轮不裁决，继续独立验收 |

## 下一步施工块

下一块不应继续压缩前台自然语言输入，而应整体处理“后台最小协议与发布调度收敛”：

1. 为回答后整理和五分钟整理建立短 Patch 协议，只输入上一稳定状态差量、已提交回合摘要、时间差和允许证据别名；在请求前计算 `prompt + reserved_output < context`，超限时由 Runtime 确定性裁剪，禁止交给服务端返回 HTTP 400。
2. 简化 R1 输出合同并增加确定性编译器，使“无记忆/单条记忆/修正记忆”成为短结构；30 个失败样本进入冻结回归集。
3. 合并和去重后台任务，限制每轮最多一个必需整理和一个可延后记忆作业；前台到来时取消低优先任务后不得立即制造多重重试风暴。
4. 修复 M2 第 55 轮的短协议失败，并将该输入及四次原始输出冻结为回归案例。
5. 在隔离 GPU 环境重跑相同 60 轮/一小时工作负载，同时记录 llama-server、检索模型和其他 GPU 进程的独立显存；只有全部闸门通过后才能把 Manifest 改为发布通过。

## 证据文件

- 原始机器报告：`report.json`
- 修正条件 Critic 旧口径后的复核报告：`reviewed_report.json`
- SYS-12 汇总裁决：`eval/world_mind_p0/sys12_minimal_input_acceptance_20260814.json`
- SYS-12 汇总说明：`eval/world_mind_p0/SYS-12最小输入正式验收_20260814.md`
- 回合证据：`turns.jsonl`
- 模型调用证据：`model_calls.jsonl`
- 资源证据：`resources.jsonl`
- 数据库：`world_mind.sqlite3`
