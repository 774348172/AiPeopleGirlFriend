# 白未晞 7B 第一阶段 V6 短烟测报告

> 结论：未通过。不得进入 60 轮或一小时发布栈验收。

## 测试对象

- 模型：白未晞 Qwen2.5-7B Q5_K_M。
- SHA256：`83cd7132c21e484f72777e2194d505c1cedea6fbbc6a2d0de913d861da81f6f3`。
- 发布路径：llama.cpp b10256，context 4096，GPU layers 28，KV cache q4_0/q4_0。
- 工作负载：冻结 10 轮 V6 短烟测，覆盖 M2、GAME_REPLY、B1、R2、连续性审查、取消、无效 JSON、重复请求、冲突、崩溃恢复、投影重建和第二女主隔离。

## 通过项

- 10/10 回合提交，20 个事件，无孤儿、重复或半提交。
- 冻结快照、最新世界版本、取消恢复、崩溃恢复和在线投影重建通过。
- 多女主状态、Prompt、任务和记忆隔离通过，交叉可见记忆为 0。
- R2 记忆作业 10/10 完成；队列峰值 10，最终排空。

## 失败项

- GAME_REPLY 10/10 均生成到 360 token 上限；回复出现重复“怕”、乱码、外语碎片和失控续写，不可用于角色对话。
- 回合延迟 P50 `11.15s`、P95/最大 `12.47s`；GAME_REPLY 平均 `9.85s`。
- B1 出现 3 个 `model_invalid_json`：2 个回答后整理、1 个五分钟整理。
- 连续性审查多次生成到 650 token 上限，平均 `19.17s`，输出 reason 出现超长无意义重复。
- 系统 GPU 峰值 `7881 MiB`、P95 `7870 MiB`，远高于冻结的 `5120 MiB` 门槛，并已接近 8GB 物理上限。
- 进程树 RSS 峰值 `8172396544` bytes。

## 根因边界

- 模型包 Modelfile 明确要求停止序列 `<|im_end|>` 和 `<|im_start|>`。
- 当前 `LlamaCppReplyModel` 的 `/v1/chat/completions` 请求未发送 `stop`。
- 按模型包 SYSTEM/模板直接运行时，模型开头能够正常回答“我是白未晞”，随后因未停止继续生成并逐渐退化。这证明权重不是完全不可生成，但当前模板/EOS/stop 合同不兼容。
- stop 缺失能解释 GAME_REPLY 失控，不能单独解释 B1 和连续性审查的结构协议失败；后两者必须在 stop 修复后单独复验。

## 下一步

1. 为 7B 候选建立显式、可测试的 chat template 与 stop 合同，不把模型专属停止序列写死为所有模型共用规则。
2. 重新执行直接对话、M2、GAME_REPLY、B1、R2 和连续性审查短测。
3. 短测全部通过后再调 GPU offload；当前 28 层配置没有资源余量。
4. 协议和资源短测通过前，不执行 60 轮或一小时验收。

## 证据

- 原始运行：`eval/world_mind_p0/runs_7b_stage1_20260814/sys11-20260814-135853/`
- 汇总报告：`eval/world_mind_p0/runs_7b_stage1_20260814/sys11-20260814-135853/report.json`
- 模型调用：`eval/world_mind_p0/runs_7b_stage1_20260814/sys11-20260814-135853/model_calls.jsonl`
- 原子回合：`eval/world_mind_p0/runs_7b_stage1_20260814/sys11-20260814-135853/turns.jsonl`
