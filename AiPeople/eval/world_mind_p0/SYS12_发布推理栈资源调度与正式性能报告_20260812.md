# SYS-12 发布推理栈、资源调度与正式性能报告

> 状态：未通过。当前配置是候选配置，不是冻结发布配置。

## 结论

- llama.cpp + 白未晞 Q4_K_M 的静态回复性能、组合显存、检索延迟和前台抢占探针通过。
- 正式 60 轮发布栈运行未通过：运行 25.1 分钟后因真实模型结构协议失败退出。
- 运行尝试到第 9 轮，但只有 2 个回合完成原子提交；不能据此宣布一小时稳定或 SYS-12 完成。
- 角色质量仍延期，本报告只裁决系统工程与发布兼容性。

## 已通过项

| 项目 | 结果 | 门槛 |
|---|---:|---:|
| 组合 GPU 峰值（完整资源探针） | 5087 MiB | < 5120 MiB |
| 正式运行观测 GPU 峰值 | 4872 MiB | < 5120 MiB |
| 热首内容 P95 | 40.96 ms | < 2000 ms |
| 80 token P95 | 2685.88 ms | < 5000 ms |
| 160 token P95 | 4960.95 ms | < 8000 ms |
| 记忆选择增量 P95 | 86.24 ms | < 300 ms |
| 前台抢占、崩溃恢复和生命周期探针 | 通过 | 必须通过 |

## 正式运行

- 运行目录：`F:\AiPeople\eval\world_mind_p0\runs_sys12_release_acceptance\sys11-20260812-211634`
- 计划回合：60；尝试到第 9 轮；正式事务：2。
- 模型调用：72；成功返回：68；重启：4；前台抢占：17；队列峰值：2。
- 上下文压缩后未再出现 llama.cpp HTTP 400，说明无界证据枚举和重复上下文问题已修复。
- 进程最终因记忆结构协议失败退出，未生成合格的一小时稳定报告。

## 阻塞项

1. `TURN_MIND_ADVANCE` 经常写满 520 token，结构化 JSON 被截断。提高上限会重新增加端到端延迟，不能只改一个数字后冻结。
2. `MEMORY_PROPOSE` 在真实轨迹中生成无效或不完整对象，包括非 RFC3339 的 `temporal.start_at`，有限重试后仍失败。
3. 当前吞吐无法在一小时内完成冻结 60 轮工作负载；静态 80/160 token 测试通过不等于完整 V6 回合性能通过。

## 下一轮要求

- 重新设计更短的心智 Patch 协议，减少固定字段和重复状态，同时保持模型语义判断与硬校验边界。
- 把记忆时间结构拆成模型可稳定输出的最小草案，再由程序做确定性格式解析；禁止程序替模型决定记忆语义。
- 用同一 60 轮冻结工作负载重跑，必须完成全部回合、持续至少一小时、队列最终排空且无不可恢复退出。
- 通过前，`local_runtime/sys12_release_manifest.json` 状态保持 `sys12_candidate_failed_acceptance`。

## 证据

- `eval/world_mind_p0/sys12_release_performance_final.json`
- `eval/world_mind_p0/sys12_combined_resource_report.json`
- `eval/world_mind_p0/sys12_release_resilience_report.json`
- `eval/world_mind_p0/runs_sys12_release_acceptance/sys11-20260812-211634/`
