# SYS-11 一小时真实模型稳定性验收报告

## 结论

`SYS-11` 系统稳定性闸门通过。角色质量闸门继续延后，发布性能闸门留待 `SYS-12`。

- 使用真实 Ollama 模型 `qinweixi-qwen35:latest` 和正式 BGE / Reranker 工件。
- 执行 60 个计划轮次，覆盖一小时真实墙钟运行；16 轮完整提交，44 轮在重试后明确失败。
- 明确失败轮均为零事务、零事件、零模型决策提交，后续轮次继续运行。
- 事务 16 个、事件 32 个、模型决策 16 个；孤儿、重复、事件数量不匹配和悬空决策均为 0。

## 故障与恢复

- 回答期间持续推进程序世界状态；冻结快照撕裂 0 次，下一轮陈旧快照 0 次。
- 取消后重试、非法 JSON 结构重试、重复请求重放和同 ID 冲突均被实际触发并通过。
- 第 24 轮实际终止完整 Worker 进程树，随后从最后提交世界线恢复；未提交草稿没有恢复。
- 第 42 轮在线删除并重建投影成功；终审阶段再次从追加账本重建成功。
- 后台队列峰值 2，最终 pending 0、running 0；前台抢占 47 次。

## 真实 AI 链

六类模型模式均有真实成功调用：

- `TURN_MIND_ADVANCE`: 194
- `WORLD_CONTINUITY_REVIEW`: 44
- `GAME_REPLY`: 16
- `POST_REPLY_WORLD_MIND_RECONCILE`: 31
- `FIVE_MINUTE_WORLD_MIND_RECONCILE`: 11
- `MEMORY_PROPOSE`: 1

记忆链实际完成 propose、materialize、正式 embedding 和索引重建。数据库中形成 1 条角色记忆、3 个选择视图和 3 个向量嵌入。

## 隔离与资源

- 合成第二女主的状态、Prompt、记忆和后台任务泄漏数为 0。
- RSS p95 约 2.19 GiB，GPU 总占用 p95 约 5.86 GiB，句柄 p95 598，资源增长判定有界。
- 成功轮端到端延迟 p50 约 21.7 秒，p95 约 48.4 秒；发布性能结论不得由本报告推出。

## 已知债务

秦未晞工程模型仅有 16/60 轮完整通过六段结构协议，44 轮明确失败。该结果证明系统能在高协议失败率下保持一致和继续运行，不代表白未晞角色质量或正式模型协议兼容已经合格。

后续 `SYS-12` 负责冻结发布推理栈、资源调度和性能；角色质量与正式模型结论仍按独立评测处理。
