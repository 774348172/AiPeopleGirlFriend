# WMR-08 工程预冻结报告

> 日期：2026-08-09  
> 范围：使用秦未晞历史模型验证 V6 工程链路  
> 结论：工程预冻结通过，正式 P0 延期

## 1. 验收边界

本次只验证 V6 Runtime 能否在真实本地模型上完成五模式调用、Schema 约束、原子回合、回答后整理、五分钟整理、性能采集和可审计冻结。

本次不验收：

- 白未晞角色质量。
- 白未晞正式回复模型。
- 正式 Embedding 与 Reranker。
- 发布栈正式延迟、组合显存、KV cache、模型切换和一小时稳定性。

## 2. 工件

- 工程合同：`wmr08_engineering_prefreeze_contract_v1.json`。
- 运行工具：`tools/run_wmr08_engineering_prefreeze.py`。
- 报告 Schema：`runtime/schemas/wmr08_engineering_report_v1.schema.json`。
- 最终报告：`runs/qin-engineering-20260809-221112/report.json`。
- 冻结清单：`runs/qin-engineering-20260809-221112/freeze_manifest.json`。
- 冻结清单 SHA256：`5665547cf4972e7d09267ba95ffe923f54883b22256cb29f0d2511eee65c4f1b`。

## 3. 工程结果

| 项目 | 结果 |
|---|---:|
| 五模式覆盖 | 5/5 |
| 前台原子回合 | 1/1 成功 |
| 后台整理任务 | 2/2 成功 |
| 后台失败 | 0 |
| Critic 逻辑覆盖 | 3/3 |
| `getLatestSnapshot()` P95 | 0.0077 ms |
| 测试替身 Repository 召回 P95 | 0.5289 ms |
| `GAME_REPLY` 首个非空内容 | 811.55 ms |
| `GAME_REPLY` 完整结构化输出 | 3526.47 ms |
| 五分钟整理端到端 | 34511.35 ms |
| Ollama 模型驻留 | 约 3023 MiB |
| 整机 GPU 已用显存采样峰值 | 4776 MiB |

这些性能样本用于证明采集链工作，不构成正式 P95 或发布结论。

## 4. 失败证据

两轮压力探针报告 `runs/qin-engineering-20260809-215701/report.json` 在第二轮 `TURN_MIND_ADVANCE` 出现结构化输出失败，评测器正确将该次运行判为失败。该失败来自秦未晞旧模型的结构稳定性，进一步说明正式稳定性必须等待白未晞模型。

## 5. 内部测试

- WMR-08 专项：7 passed。
- `tests/world_mind + tests/runtime`：307 passed，3 skipped。
- 全仓：668 passed，3 skipped；36 failed，5 errors。

全仓失败来自 V5 和历史训练冻结清单仍绑定已经升级的权威需求、AI 设计和角色资产哈希。按照 V6 施工总纲，不得为通过旧冻结测试而恢复旧文档或旧角色设计。

## 6. 最终结论

`engineering_gate=passed`，`formal_p0_gate=deferred`。

下一次正式验收必须接入白未晞回复模型、正式 BGE、正式 Reranker 和目标发布栈，再执行角色质量、正式样本 P95、组合显存、模型切换、KV cache 和一小时稳定性。
