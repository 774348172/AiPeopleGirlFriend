# 7B 运行时兼容层与短协议复验报告

日期：2026-08-14  
模型：白未晞 Qwen2.5-7B Q5_K_M  
SHA256：`83cd7132c21e484f72777e2194d505c1cedea6fbbc6a2d0de913d861da81f6f3`

## 1. 结论

本轮完成了 7B 模型专属运行时兼容层。M2、B1、R2 三个结构化短协议通过；GAME_REPLY 的模板和停止序列传输问题已修复，但模型正文仍发生角色续写、外语碎片和乱码，因此 7B 候选仍不通过。

当前不得进入 10/60 轮或一小时正式验收。下一步应修复或更换 7B 模型产物，再复用本报告中的同一探针；不应通过缩短 token 上限、接受截断文本或程序拼接假对白掩盖失败。

## 2. 已施工兼容合同

- `model.runtime_compatibility.chat_template = chatml`
- `model.runtime_compatibility.stop_sequences = [<|im_end|>, <|im_start|>]`
- `model.runtime_compatibility.structured_output = json_schema`
- llama.cpp 启动时显式传递 `--chat-template chatml`。
- 每次 HTTP 生成请求显式传递模型停止序列。
- 正式生成收到 `finish_reason=length` 时抛出协议错误，不提交截断结果。
- 仅内部预热允许以 token 上限结束，因为预热输出不会显示或提交。

## 3. 回归结果

适配器与 SYS-12 发布合同：`44 passed`。

覆盖内容：

- 模型专属模板与停止序列从发布清单加载并传到运行时。
- llama.cpp 启动参数绑定声明模板。
- HTTP 请求包含停止序列。
- `finish_reason=length` 被识别为确定性协议失败。
- 启动、预热、SSE、取消、恢复、并发串行和资产校验无持续回归。

## 4. 短协议探针

报告：`eval/world_mind_p0/7b_runtime_compatibility_probe_20260814.json`

| 模式 | 结果 | 生成 token | 耗时 |
|---|---:|---:|---:|
| MIND_PATCH_V2 / M2 | 通过，`{"c":[]}` | 16 | 1070.05 ms |
| B1 | 通过，`{"c":[],"m":[]}` | 33 | 1793.97 ms |
| R2 | 通过，`{"m":[]}` | 17 | 1137.53 ms |
| GAME_REPLY | 质量失败 | 140 | 2903.94 ms |

GAME_REPLY 虽在上限前结束，但出现 `assistant/assis`、外语碎片、乱码和替双方继续对话，不能作为角色回复提交。

## 5. 真实一回合链路

运行目录：`eval/world_mind_p0/runs_7b_compat_one_turn_20260814/sys11-20260814-151847/`

- 两次回合尝试中的 M2 均成功，分别生成 14 和 17 token。
- 四次 GAME_REPLY 均打满 360 token，被新的长度截断保护拒绝。
- 回合提交数为 0，事件和模型决策无部分提交，说明失败关闭正确。
- GAME_REPLY 阶段已阻断，因此该运行没有进入 B1/R2 后台阶段；B1/R2 由独立短协议探针验证。

该负载仅用于一回合兼容诊断，报告中的一小时门槛、崩溃注入和 60 轮计数失败属于预期，不作为本轮判断依据。

## 6. Ollama 差分

同一 GGUF 使用模型包原始 Modelfile，经 Ollama 回答“你是谁？”：

- 生成 64/64 token，`done_reason=length`。
- 前半段能回答身份，随后开始替男主续写下一轮对白。
- 与 llama.cpp 的退化方向一致。

因此，缺少模板/stop 是第一阶段的真实运行时缺陷，但修复后剩余 GAME_REPLY 主故障属于当前 7B 模型产物的回合终止学习和对白质量，不是 llama.cpp 独有问题。

## 7. 发布判定

状态：`sys12_7b_candidate_compatibility_partial`

- 运行时兼容层：通过。
- M2/B1/R2 短协议：通过。
- GAME_REPLY：不通过。
- 10/60 轮、一小时验收：未执行，等待模型产物通过 GAME_REPLY 最小门槛。
- GPU 层数调优：未执行；当前模型协议与质量失败时调资源没有发布价值。
