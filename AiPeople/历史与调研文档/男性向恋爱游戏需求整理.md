# 男性向恋爱游戏 —— 需求整理

> 性质：需求整理文档（非实施）。基于用户口述 + 之前所有研究结论，系统化梳理。
> 日期：2026-08-03。

---

## 0. 一句话描述

一个男性向恋爱模拟游戏，玩家与虚拟女友自由对话，女友根据对话内容/语气调用动作、语音等接口做出反应。模型跑在玩家本地（3060 8GB），不联网，保护隐私。

---

## 1. 核心需求

### 1.1 产品形态
- **类型**：男性向（男玩家）恋爱模拟游戏
- **核心玩法**：与虚拟女友自由文字对话 + 女友情感反馈（表情/动作/语音）
- **平台**：PC 端（Windows）
- **分发**：游戏本体 + LLM 模型文件一起 ship 给玩家

### 1.2 硬性约束

| 约束 | 值 | 来源 |
|---|---|---|
| 玩家最低 GPU | RTX 3060 8GB | 用户指定 |
| 联网 | ❌ 不联网 | 隐私保护 |
| 隐私 | 对话数据不出本地 | 用户要求 |
| 训练 | 在 H200 上做（dev 端） | 不在玩家机器训练 |
| 推理 | 在玩家 3060 8GB 上跑 | 用户要求 |

### 1.3 用户体验目标

| 维度 | 目标 |
|---|---|
| 对话自由度 | 玩家可以自由输入任何话题，不限选项 |
| 人格一致性 | 虚拟女友有稳定人格、口吻、背景故事 |
| 情感反馈 | 根据玩家说话内容和语气，女友做出不同情绪反应 |
| 动作调用 | 根据对话/语气，调用预设的动作接口（表情/肢体/场景） |
| 语音 | 女友有语音输出（可选/可关） |
| 响应速度 | 文字回复 <2-3 秒，让对话流畅 |

---

## 2. 技术架构

### 2.1 训练端（H200）

```
H200 dev 机器
  └─ Qwen3-4B 全精度 LoRA SFT（虚拟女友人格）
     ├─ 数据：人生语料 + 闲聊 + 身份锚 + 恋爱场景 + 情绪标注样本
     └─ 导出 GGUF Q4_K_M（~2.3GB）→ ship 给玩家
```

### 2.2 玩家端（3060 8GB）

```
玩家 PC (3060 8GB, Windows)
  ┌─ 游戏引擎（UE/Unity/Godot）
  │   ├─ 渲染场景 + 虚拟女友形象（Live2D/3D）
  │   ├─ 文字输入框
  │   ├─ 表情/动作系统（预设接口）
  │   └─ 语音播放系统
  │
  ├─ LLM 推理服务（Ollama / llama.cpp，localhost）
  │   ├─ 加载 girlfriend_4b_q4.gguf（~2.3GB, ~2.5GB 显存）
  │   ├─ 接收玩家输入 → 生成女友回复 + 情绪标签
  │   └─ 输出：回复文本 + emotion + action
  │
  └─ 显存分配（8GB）
      ├─ LLM 推理：~2.5GB
      ├─ 游戏+桌面：~4.5-5.5GB
      └─ 语音模型（如需本地 TTS）：~0.5-1GB（可选）
```

### 2.3 关键数据流

```
玩家输入文字
  → 游戏引擎传给 LLM（localhost HTTP）
  → LLM 生成回复 + 情绪标签 + 动作标签
  → 游戏引擎解析标签
  → 调用动作接口（表情/肢体/场景切换）
  → 调用语音接口（TTS 合成语音）
  → 展示：文字 + 动作 + 语音
```

---

## 3. 虚拟女友设计（待定项）

### 3.1 角色设定（需你定义）

| 项 | 说明 | 状态 |
|---|---|---|
| 名字 | 女友叫什么 | 待定 |
| 年龄/外貌 | 人设（发色/瞳色/体型/穿搭风格） | 待定 |
| 性格 | 大五人格 + 特质（温柔/傲娇/元气/病娇/...） | 待定 |
| 背景故事 | 人生 timeline | 待定 |
| 口吻特征 | 说话风格/口头禅/语气词 | 待定 |
| 与玩家关系 | 初始关系（陌生人/同班/同事/青梅/...） | 待定 |
| 情感发展 | 好感度系统？几个阶段？ | 待定 |

### 3.2 性格类型参考（男性向常见）

| 类型 | 特征 | 口吻示例 |
|---|---|---|
| 温柔型 | 体贴、包容、慢热 | "嗯……你回来啦，今天辛苦了吧。" |
| 傲娇型 | 嘴硬心软、容易害羞 | "才、才不是在等你呢！只是刚好路过……" |
| 元气型 | 开朗、主动、爱撒娇 | "嘿嘿，你终于回我了！今天有什么好玩的事吗？" |
| 病娇型 | 占有欲强、偏执 | "你刚才……是在跟别人说话吗？" |
| 三无型 | 冷淡寡言、偶尔破功 | "……嗯。" "……不是在意你。" |
| 大姐姐 | 成熟、宠溺、从容 | "没关系，慢慢来，我等你。" |

### 3.3 情绪/动作接口（需你定义）

LLM 输出格式示例：

```json
{
  "reply": "你……今天怎么突然这么客气，有点奇怪……不过，还挺开心的。",
  "emotion": "shy",           // 情绪标签
  "action": "look_away",      // 动作接口
  "expression": "blush",      // 表情接口
  "voice_tone": "soft"        // 语音语调
}
```

情绪标签体系（需你定义）：

| 类别 | 可能标签 |
|---|---|
| 正面 | happy, shy, tender, playful, excited, love |
| 负面 | sad, angry, jealous, worried, hurt, cold |
| 中性 | calm, thinking, surprised, embarrassed, speechless |

动作/表情接口（需游戏引擎侧定义）：

| 接口 | 说明 | 示例值 |
|---|---|---|
| expression | 面部表情 | smile, blush, pout, frown, tears, surprised |
| body | 肢体动作 | lean_in, look_away, cross_arms, hold_hand, turn_back |
| scene | 场景效果 | weather_change, lighting_change |
| voice_tone | 语音语调 | soft, cheerful, cold, trembling, whisper |

---

## 4. 训练数据需求

### 4.1 数据类型

| 类型 | 说明 | 教模型什么 | 占比建议 |
|---|---|---|---|
| 人生语料 | 女友的日记/回忆/经历 | canon 事实、自传记忆 | ~15% |
| 身份锚 | "你叫什么""你多大"等 | 身份问题不崩 | ~5% |
| 自由闲聊 | 和玩家聊日常/兴趣/观点 | 自由对话（核心） | **~30%** |
| 恋爱场景 | 调情/撒娇/吃醋/吵架/和解/表白 | 恋爱语境口吻 | **~25%** |
| 情绪标注 | 带情绪/动作标签的对话样本 | 输出结构化情绪+动作 | **~20%** |
| 越界保护 | 政治/隐私/不当话题婉拒 | 不破人格不胡编 | ~5% |

### 4.2 之前版本的经验教训（直接适用）

| 教训 | 对应措施 |
|---|---|
| 单轮 Q&A 教不出自由对话 | 闲聊+恋爱场景占比 ≥50% |
| 短回复导致无逻辑 | 过滤 <10 字回复 |
| 少量闲聊被淹没 | 不用大量结构化数据淹没闲聊 |
| 1.7B 多轮弱 | 用 4B（H200 训练 → 4B Q4 ship） |
| canon 漂移 | 加事实一致性样本 |
| 套模板 | 多样化生成（不同温度/场景/prompt 变体） |

### 4.3 男性向恋爱游戏特有需求

| 特有需求 | 说明 |
|---|---|
| 恋爱语境 | 需要调情/暧昧/吃醋/撒娇/表白/约会等场景的训练数据 |
| 情感发展 | 不同好感度阶段不同说话方式（初识客套→熟络→暧昧→确认关系→深恋） |
| 情绪标签 | 训练数据需带 emotion/action 标签，教模型输出结构化情绪 |
| 玩家称呼 | 女友对玩家的称呼随好感度变化（"你"→名字→昵称） |
| 尺度边界 | 亲密但不越界——需 Protective Scene 设计 |

### 4.4 情绪标签训练数据格式

训练数据需要同时教模型"说话"和"输出情绪标签"。sharegpt 格式扩展：

```json
{
  "conversations": [
    {"from": "human", "value": "今天好累啊……"},
    {"from": "gpt", "value": "【emotion:shy】【action:lean_in】怎么了？要不要我帮你揉揉肩……才不是关心你，只是看你这样子怪可怜的。"}
  ]
}
```

或用 system prompt 教模型输出 JSON：

```json
{
  "conversations": [
    {"from": "human", "value": "今天好累啊……"},
    {"from": "gpt", "value": "{\"reply\":\"怎么了？要不要我帮你揉揉肩……才不是关心你。\",\"emotion\":\"shy\",\"action\":\"lean_in\"}"}
  ]
}
```

**格式需你决策**——前缀标签法（简单、4B 容易学）vs JSON 输出法（灵活、但 4B 可能不稳）。

---

## 5. 好感度系统（需决策）

| 方案 | 说明 | 对训练的影响 |
|---|---|---|
| 无好感度 | 对话风格始终一致 | 最简单，一个 adapter |
| 线性好感度 | 随对话轮数/关键词推进 | 需要分阶段训练数据（初期/中期/后期） |
| 剧情节点 | 特定事件触发关系升级 | 需要剧情 gated 样本（之前 D 方案） |
| 混合 | 好感度 + 剧情节点 | 最丰富但训练数据量最大 |

如果分阶段，可能需要：
- 一个 adapter + 不同 system prompt 切阶段（轻量）
- 或多个 adapter（初期/中期/后期各一个，热切换）

---

## 6. 语音方案（需决策）

| 方案 | 显存 | 优点 | 缺点 |
|---|---|---|---|
| 本地 TTS（如 edge-tts/piper/GPT-SoVITS） | ~0.5-1GB | 不联网、任意文字可合成 | 音色固定/额外显存 |
| 预录制语音（按情绪切片） | ~0GB | 零显存、音色好 | 只能说预设台词 |
| 混合（预录制常用 + TTS 兜底） | ~0.5GB | 平衡 | 工程复杂 |
| 纯文字 | 0GB | 最简单 | 体验降级 |

3060 8GB + 游戏(~3GB) + LLM(~2.5GB) = 5.5GB，TTS 再占 0.5-1GB = 6-6.5GB——还能跑（8GB 剩 1.5-2GB）。

**GPT-SoVITS** 特别适合这个场景——可以定制女声音色，但显存占用偏大（~1-2GB），8GB 上偏紧。

---

## 7. 游戏引擎集成

### 7.1 LLM 与游戏引擎的通信

```
游戏引擎 ←→ LLM 推理服务（localhost HTTP API）

请求: POST /chat {"message": "玩家输入", "history": [...]}
响应: {"reply": "...", "emotion": "shy", "action": "lean_in", "expression": "blush"}
```

### 7.2 推理服务选择

| 方案 | 显存 | 适合 |
|---|---|---|
| **Ollama + GGUF** | ~2.5GB | 玩家分发（最简） |
| llama.cpp server | ~2.3GB | 嵌入游戏（更底层） |
| NVIGI（进程内） | 最优 | NVIDIA 专属最优 |

### 7.3 延迟预算

| 环节 | 预算 |
|---|---|
| 玩家输入 → LLM | <100ms |
| LLM 生成（~50 token） | ~1-2s（3060 4B Q4） |
| 情绪解析 + 动作调用 | <50ms |
| TTS 合成（如需） | ~0.5-1s |
| **总延迟** | **~2-3s** |

---

## 8. 需要你决策的待定项

| # | 决策项 | 选项 | 影响 |
|---|---|---|---|
| 1 | 女友人设 | 名字/性格类型/背景/外貌 | 决定训练数据内容 |
| 2 | 性格类型 | 温柔/傲娇/元气/病娇/三无/大姐姐 | 决定口吻和训练数据风格 |
| 3 | 情绪标签体系 | 用哪套情绪/动作标签 | 决定训练数据格式 |
| 4 | 情绪输出格式 | 前缀标签 vs JSON | 决定 sharegpt 格式 |
| 5 | 好感度系统 | 无/线性/剧情节点/混合 | 决定训练数据量和 adapter 数 |
| 6 | 语音方案 | 本地TTS/预录制/无 | 决定显存预算 |
| 7 | 游戏引擎 | UE/Unity/Godot | 决定集成方式 |
| 8 | 女友可自定义 | 固定人设/玩家可调名字性格 | 决定权重 vs prompt 架构 |
| 9 | 尺度边界 | 到什么程度 | 决定 Protective Scene 内容 |
| 10 | 单/多女友 | 一个/多个可选 | 决定训练几个 adapter |

---

## 9. 之前工作的复用

| 已有资产 | 复用到哪 |
|---|---|
| data_gen 管线 | 生成女友的人生语料 |
| casual 闲聊生成器 | 生成女友的闲聊数据（改 prompt 即可） |
| identity 锚生成器 | 生成女友的身份问答 |
| Protective Scene | 生成女友的越界保护 |
| LLaMA-Factory SFT 管线 | 训练女友人格 |
| export_gguf.py | 导出 GGUF ship 给玩家 |
| H200 训练包 | 在 H200 上训女友人格 |
| 版本效果对比文档 | 避免重蹈覆辙 |

**需新增**：
- 恋爱场景数据生成器（调情/暧昧/吃醋/表白等场景）
- 情绪标注数据生成器（带 emotion/action 标签的对话样本）
- 好感度阶段数据（如选分阶段）

---

## 10. 与之前于谦/林知微的区别

| 维度 | 于谦/林知微 | 女友（现在） |
|---|---|---|
| 目标用户 | 通用 | **男性玩家** |
| 角色 | 真人/虚构普通人 | **虚拟恋爱对象** |
| 对话风格 | 捧哏/插画师 | **恋爱语境（撒娇/吃醋/暧昧）** |
| 情绪输出 | 无 | **有**（emotion + action 标签） |
| 好感度 | 无 | **可能有**（分阶段说话方式） |
| 尺度 | 无限制 | **有边界**（亲密但不越界） |
| 语音 | 无 | **可能有** |
| 动作接口 | 无 | **有**（表情/肢体/场景） |
| 训练数据 | 相声/日记 | **恋爱对话 + 情绪标注** |

**最大区别**：女友需要情绪标签 + 动作接口 + 恋爱语境——LLM 不只输出文字，还要驱动游戏内的表情/动作/语音。
