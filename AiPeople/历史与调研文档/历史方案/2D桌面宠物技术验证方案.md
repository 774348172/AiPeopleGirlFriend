# 夏晚晴 2D 桌面宠物 —— 技术验证方案

> 性质：技术方案评估（非实施）。从"完整游戏"缩小到"2D 桌面宠物"验证核心链路。
> 日期：2026-08-03。只做夏晚晴一个。

---

## 0. 为什么桌面宠物是正确的验证起点

| 维度 | 完整游戏 | 2D 桌面宠物 |
|---|---|---|
| 开发周期 | 数月 | **数天-数周** |
| 需要验证的核心 | LLM 对话 + 情绪 + 角色 | **同样的核心** |
| 多余的部分 | 游戏世界/剧情/关卡/经济系统 | **全砍掉** |
| 风险 | 高（做完了才知道行不行） | **低（快速验证核心链路）** |

桌面宠物 = **砍掉一切非核心**，只留"能和角色自由对话 + 角色有表情反应"。这恰好是技术风险最高的部分——验证它就够了。

---

## 1. 当前大模型方案 vs 桌面宠物需求

### 大模型方案不变的部分

| 组件 | 方案 | 桌面宠物需要？ |
|---|---|---|
| 基座 Qwen3-4B Q4 GGUF | 2.3GB, 2.5GB 显存 | ✅ 不变 |
| Ollama/llama.cpp 本地推理 | localhost HTTP | ✅ 不变 |
| qwen3_nothink 关思考 | 直接出正文 | ✅ 不变 |
| 情绪标签输出 | emotion + action | ✅ 不变（驱动表情切换） |
| 人格 LoRA SFT 训练 | H200 全精度 → Q4 ship | ✅ 不变 |
| 训练数据：恋爱场景+闲聊+情绪标注 | — | ✅ 不变 |

**结论：大模型链路完全不用改。** 桌面宠物只是换了前端展示方式。

### 需要调整的部分

| 组件 | 原方案（游戏） | 桌面宠物方案 |
|---|---|---|
| 前端 | Unity + Live2D（重） | **轻量桌面 app**（Python/Electron） |
| 角色表现 | 3D/高品质 Live2D + 场景 | **2D 立绘 + 表情切换**（简单） |
| 交互方式 | 游戏内输入框 | **桌面悬浮窗 + 文字气泡** |
| 语音 | piper TTS | 可选，先纯文字 |
| 分发 | Steam | 先本地验证，不分发 |
| 游戏世界 | 需要 | **完全不需要** |

---

## 2. 桌面宠物技术架构

```
玩家桌面（3060 8GB, Windows）
  ┌─ 桌面宠物 app（轻量，Python PyQt 或 Electron）
  │   ├─ 透明置顶窗口——2D 角色立绘悬浮在桌面上
  │   ├─ 文字输入框（点角色或快捷键唤出）
  │   ├─ 对话气泡（角色说话时显示）
  │   ├─ 表情切换（根据 emotion 标签切换立绘图层）
  │   └─ 简单动作（站立/走动/转头/叉腰）
  │
  ├─ LLM 推理（Ollama, localhost:11434）
  │   ├─ 加载 xiawanqing_4b_q4.gguf（2.3GB）
  │   ├─ 接收文字 → 生成回复 + 情绪标签
  │   └─ 返回 JSON：{reply, emotion, action}
  │
  └─ 显存（8GB）
      ├─ LLM：~2.5GB
      ├─ 桌面 app + 2D 立绘：~0.3GB
      └─ 系统/桌面：~5GB（很宽松）
```

**2D 桌面宠物比游戏省很多显存**——不需要 3D 渲染、不需要场景、不需要贴图。LLM 的 2.5GB 是大头，其余几乎不占。

---

## 3. 实时对话可行性分析

### 延迟预算（桌面宠物）

| 环节 | 耗时 | 说明 |
|---|---|---|
| 用户输入 → Ollama HTTP | ~10ms | 本地 localhost |
| LLM 生成首 token（TTFT） | ~200-400ms | 4B Q4 在 3060 上 |
| LLM 生成 ~30-50 token | ~1-2s | ~25-35 tok/s |
| 情绪解析 + 表情切换 | ~10ms | 切换立绘图层 |
| **总延迟** | **~1.5-2.5s** | 用户发消息到看到回复+表情变化 |

**2D 桌面宠物可以做到实时对话**——1.5-2.5s 的延迟在文字聊天里完全可接受（微信回复也就这个速度）。

### 能不能更快？

| 优化手段 | 效果 | 代价 |
|---|---|---|
| 流式输出（逐字显示） | TTFT 后立刻开始显示，体感延迟降到 ~0.5s | Ollama 支持 stream 模式 |
| 更小模型（1.7B Q4） | 快一倍，~0.5-1.5s | 人格质量下降（之前验证过） |
| 更短 max_tokens | 更快 | 回复太短 |
| 预加载/常驻 | 首次加载 30s，之后秒级响应 | — |

**推荐：流式输出**——Ollama 支持 stream，LLM 一边生成一边显示文字，用户体感延迟 ~0.5s（首字出现时间），不用等整句生成完。这是实时对话的最佳体验。

---

## 4. 桌面宠物前端方案选择

| 方案 | 语言 | 2D 能力 | LLM 集成 | 开发速度 | 推荐 |
|---|---|---|---|---|---|
| **Python + PyQt6** | Python | 透明窗口+图层叠加 | 直接调 Ollama HTTP | 快（1-2 天原型） | ✅ **首选** |
| Electron | JS/HTML | 好（HTML/CSS 动画） | fetch Ollama | 中（2-3 天） | 可选 |
| C# + WPF | C# | 好 | HTTP | 中 | 可选 |

**推荐 Python + PyQt6**——理由：
- 和我们现有 Python 管线完全一致（data_gen/training 都是 Python）
- 透明窗口 + 置顶 + 图层叠加 PyQt 原生支持
- 直接 import requests 调 Ollama localhost
- 开发最快（1-2 天出原型）
- 后期如果要升级到 Electron/Unity 也容易迁移

### 夏晚晴的 2D 表现方式（简化版）

不需要 Live2D——用**多表情立绘 + 图层切换**即可：

| 情绪 | 立绘变化 | 实现 |
|---|---|---|
| playful | 叉腰 + 得意笑 | 切到 playful 立绘 |
| shy | 脸红 + 转头 | 切到 shy 立绘 |
| tender | 突然安静 + 看你 | 切到 tender 立绘 |
| jealous | 叉腰 + 鼓腮帮 | 切到 jealous 立绘 |
| sad | 安静 + 眼眶红 | 切到 sad 立绘 |
| angry | 不看你 + 背身 | 切到 angry 立绘 |

6-8 张不同表情的立绘 → 根据 LLM 输出的 emotion 标签切换。比 Live2D 简单 10 倍，验证阶段够用。

后期升级 Live2D 时，Live2D Cubism SDK 有 Python 绑定（或切 Electron 用 Web Live2D），升级路径清晰。

---

## 5. LLM 输出格式（为桌面宠物适配）

### 推荐格式：前缀标签法

```json
{
  "conversations": [
    {"from": "human", "value": "你怎么又迟到了"},
    {"from": "gpt", "value": "【emotion:playful】【action:hand_on_hip】喂！你才迟到呢！我等了你好久了好不好……哼，算了不跟你吵了，走吧。"}
  ]
}
```

**为什么选前缀标签而不是 JSON**：
- 4B 模型学 JSON 输出不稳定（之前经验：4B 输出 JSON 有时不闭合/格式错）
- 前缀标签 `【emotion:xxx】` 简单直接，4B 容易学会
- 桌面宠物 app 解析前缀标签也简单（正则提取）
- 即使标签解析失败，正文文字照样能显示——容错好

### 桌面宠物 app 解析逻辑

```python
import re

def parse_response(text):
    # 提取情绪标签
    emotion_match = re.search(r'【emotion:(\w+)】', text)
    action_match = re.search(r'【action:(\w+)】', text)
    # 去掉标签，保留正文
    reply = re.sub(r'【\w+:\w+】', '', text).strip()
    return {
        "reply": reply,
        "emotion": emotion_match.group(1) if emotion_match else "calm",
        "action": action_match.group(1) if action_match else "stand"
    }
```

---

## 6. 夏晚晴训练数据需求（桌面宠物验证版）

### 数据量：先做最小可验证集

| 类型 | 条数 | 说明 |
|---|---|---|
| 自由闲聊 | 100 条 | 日常话题（天气/吃/工作/睡/心情） |
| 恋爱场景 | 80 条 | 撒娇/吃醋/暧昧/表白/吵架/和好 |
| 身份锚 | 30 条 | "你叫什么""你多大""你和我什么关系" |
| 情绪标注 | 50 条 | 带 【emotion:xxx】【action:xxx】 的对话 |
| 越界保护 | 20 条 | 不当话题婉拒 |
| **合计** | **280 条** | 比于谦的 5554 少很多，但验证够用 |

### 关键：情绪标注数据怎么生成

用 glm-5.2 生成带标签的对话——prompt 教模型输出"前缀标签 + 正文"格式：

```
生成一段你和"你"（你的青梅竹马）的对话。
话题：{topic}
要求：你的每条回复前面加【emotion:情绪】【action:动作】，然后是正文。
情绪可选：playful/shy/tender/jealous/sad/angry
动作可选：hand_on_hip/turn_away/cover_mouth/puff_cheeks/calm_down/look_at_you
```

### 好感度阶段（简化版）

先不分 5 阶段，只做 2 阶段验证：
- **阶段 1：日常吵嘴**（斗嘴/互怼/嬉闹，不露真心）
- **阶段 2：真心话**（玩笑里混真情/偶尔认真/暧昧）

---

## 7. 桌面宠物 app 功能清单（MVP）

### P0（必须有）

- [ ] 透明置顶窗口，2D 角色立绘显示在桌面上
- [ ] 点角色/快捷键唤出文字输入框
- [ ] 输入文字 → 调 Ollama → 收到回复
- [ ] 流式输出（逐字显示文字气泡）
- [ ] 解析 emotion 标签 → 切换立绘表情
- [ ] 对话历史（最近 6 轮）

### P1（应该有）

- [ ] 角色在桌面上简单走动（左右移动）
- [ ] 角色待机动画（等待时的小动作：吃棒棒糖/看手机/哼歌）
- [ ] 空闲超时主动搭话（"喂，你怎么不理我"）

### P2（后期）

- [ ] TTS 语音输出
- [ ] 好感度系统
- [ ] 更多表情/动作
- [ ] 角色自定义

---

## 8. 与当前已有工作的关系

### 不用改的

| 已有 | 说明 |
|---|---|
| LLaMA-Factory 训练管线 | 不变 |
| export_gguf.py | 不变 |
| train_gpu_limit.py | 不变 |
| Ollama 推理方式 | 不变 |
| qwen3_nothink 模板 | 不变 |

### 需要新增的

| 新增 | 说明 |
|---|---|
| 夏晚晴 bible.yaml | 人设档案（已有设定，转成 yaml） |
| 夏晚晴 canon.json | 事实正典 |
| 夏晚晴 timeline.yaml | 人生经历（青梅竹马版） |
| 夏晚晴 casual prompt | 闲聊生成（改口吻为晚晴的） |
| 夏晚晴 恋爱场景 prompt | 撒娇/吃醋/表白场景生成 |
| 夏晚晴 情绪标注 prompt | 带 emotion/action 标签的对话生成 |
| 桌面宠物 app（PyQt6） | 前端：透明窗口+立绘+输入+流式输出+表情切换 |

### 需要砍的

| 砍掉 | 理由 |
|---|---|
| 三女友并行 | 先做一个 |
| Unity/Live2D 集成 | 2D 立绘切换够用 |
| Steam 发行 | 先验证不分发 |
| 游戏世界/剧情/关卡 | 桌面宠物不需要 |
| TTS（P0 阶段） | 先纯文字 |
| 3D 角色 | 2D 够验证 |

---

## 9. 验证标准（什么时候算"验证通过"）

| # | 验证项 | 通过标准 |
|---|---|---|
| 1 | LLM 实时对话 | 用户发消息 → 1.5-2.5s 内开始流式输出 |
| 2 | 人格口吻 | 夏晚晴的"喂/哼/诶/嘻嘻"风格稳定 |
| 3 | 情绪驱动 | LLM 输出 emotion 标签 → 立绘表情切换 |
| 4 | 自由对话 | 不套模板、能跑题、能反问、能斗嘴 |
| 5 | 青梅竹马感 | 能自然提起"小时候的事""十五年了" |
| 6 | 持续可用 | 3060 8GB 上跑 1 小时不崩、桌面不卡 |
| 7 | 显存占用 | LLM + app 总计 <3GB，留 5GB 给系统 |

---

## 10. 工作流（验证阶段）

```
Step 1: 写夏晚晴 persona（bible/canon/timeline）          [已有设定，转格式]
Step 2: 生成训练数据（闲聊+恋爱+身份+情绪标注 ~280条）      [用 glm-5.2]
Step 3: H200 训练 Qwen3-4B Q4 → 导出 GGUF                 [已有管线]
Step 4: 写桌面宠物 app（PyQt6 透明窗口+立绘+输入+流式）     [新写，1-2天]
Step 5: 装 Ollama → 加载 GGUF → 连桌面 app               [本地部署]
Step 6: 对话验证（7 项验证标准）                           [实测]
```

Step 1-3 用已有管线，Step 4 是唯一新写的代码（桌面宠物前端）。

---

## 11. 一句话

大模型方案不用改——桌面宠物只是换了前端。先做夏晚晴一个、2D 立绘切换、PyQt6 透明窗口、流式输出、Ollama 本地推理。核心验证的是"LLM 对话 + 情绪驱动表情"这条链路能不能在 3060 8GB 上跑通。
