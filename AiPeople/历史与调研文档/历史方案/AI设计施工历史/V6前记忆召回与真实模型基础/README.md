# V6 前记忆、召回与真实模型基础

本目录保存 V6 之前已经完成或冻结的记忆与本地模型工程合同。

## 可以迁移复用

- 追加式记忆转换和证据原文校验。
- Memory materialization。
- Embedding、exact TopK 和 Reranker。
- 后台队列、重试和前台优先经验。
- 本地 BGE、Reranker 和 llama.cpp 工件验真基础。

## 已被 V6 替代

- `conversation_id` 作为记忆所有权。
- 全局跨女主候选池和先搜索后过滤。
- 秦未晞固定模型资产身份。
- 单次回复模型作为完整心智主干。
- 旧冻结文档作为现行上游权威。

复用必须通过当前施工总纲的 `WMR-04`、`WMR-05` 和 `WMR-08` 重新施工与冻结。
