# RelationshipRuntime 历史阶段施工

本目录保存旧 `RelationshipRuntime` 阶段 0～6 的施工和验收文档。

这些文件用于理解现有代码中的：

- SQLite 事件账本。
- FakeModel 纵向闭环。
- llama.cpp 流式 Adapter。
- epoch、工作激活区和确定性召回。
- 旧前瞻计划状态。

V6 不继续把 `RelationshipRuntime` 扩展为顶层心智 Runtime。现有能力只能按当前施工总纲抽取或适配到新的 `WorldMindRuntime`。
