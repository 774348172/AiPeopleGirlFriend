# 正典单副本：物理文件只存在于仓库根 人物设定/。
# AiPeople/人物设定 与 AiPeopleCreate/人物设定 是指向它的 Windows junction，
# 内容由根目录 人物设定/ 跟踪，junction 路径本身不进入 git。
/AiPeople/人物设定/
/AiPeopleCreate/人物设定/

# 训练优化器状态不再入库（2026-08-18 已从历史清理，防止再次误提交）
optimizer.pt

# Python 缓存
__pycache__/
*.pyc

# 本地密钥（只提交 .env.example，不提交真实 .env）
.env
**/.env
!.env.example

# 质量评测运行产物（jsonl + sqlite）：仅早期 8-17 批次在库，后续 run 不入库
AiPeople/eval/baiweixi_quality/runs/

# 本地模型、训练输出、临时评测与 Unsloth 编译缓存不属于源码发布。
# 正式模型身份由 local_runtime 的 Manifest 和 release note 中的 SHA256 记录。
unsloth_compiled_cache/
**/unsloth_compiled_cache/
AiPeople/models/
AiPeople/training_packages/models/
AiPeople/training_packages/**/outputs/
AiPeople/training_packages/**/*.log
AiPeople/eval/world_mind_p0/
AiPeople/**/*.sqlite*

# 训练输出备份（含模型权重，GB 级），不入库
**/outputs_bak_*/
