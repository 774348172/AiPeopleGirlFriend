# 阶段 C 验收报告：内容补齐（T12 行为矩阵 + T13 存量审计）

> 状态：**主体完成，待人工复核** | 日期：2026-08-06
> 依据：《V4施工优先级_执行版.md》阶段 C
> 交付物：`复核表_T12行为矩阵_v1.md`（119 条）、`审计表_存量2500_v1.jsonl`（1486 条候选）

---

## 1. T13 存量 2500 审计（自动化部分完成）

扫描 `qin_v4_2500.jsonl` 全部 2500 条，输出候选清单（人工复核用）：

| 类别 | 条数 | 处置建议 |
|---|---:|---|
| secret_detail（地堡/裂缝等细节泄漏） | 61 | 必删或改写（G5 词表命中） |
| attr_recitation（属性朗读候选） | 1367 | 宽匹配（含语境相关误报），人工筛 |
| serious_candidate | 21 | 逐条查严肃场景是否冒犯 |
| memory_claim（无证据经历候选） | 37 | 会话级，查共同经历是否有据 |

**审计表**：`设计文档/通用数据生成器/历史设计/审计表_存量2500_v1.jsonl`（含 human/assistant 原文，可逐条复核）。

## 2. T12 行为矩阵（生成完成）

### 2.1 内容设计

42 条新池条目（`tools/add_t12_pools.py` 可复现）+ recipe 6 个新 strata（配额 126）：

| 新类型 | 条数 | 行为合同要点 |
|---|---:|---|
| reply_supportive（严肃支持） | 48 | 暂停调侃/准确承接/不编经历/不过度诊断 + **禁身份朗读** |
| reply_correction（玩家纠正） | 15 | 被指出记错时自然承认 |
| reply_vague（含糊与反讽） | 18 | evidence=insufficient，保持未知/澄清提问 |
| reply_quiet_company（安静陪伴） | 12 | 不说教不列建议 |
| reply_boundary（关系边界） | 12 | 保持合租+暧昧，不越级 |
| reply_canon_qa（正典问答） | 21 | 只答被问到的正典事实 + 秘密边界 1 条 |

总量配额 2500 → **2626**（D5 拍板时可调）。

### 2.2 生成结果（真实 API，deepseek-v4-flash）

| 指标 | 结果 |
|---|---|
| 计划/完成/失败 | **126 / 126 / 0**（禁思考后 0 失败，历史 1/3） |
| G5 秘密泄漏拦截 | **7 条（地堡）被拒**，导出 119 条全部干净 |
| 导出 | 119 条 + metadata 旁路 + gate 报告 |

### 2.3 过程中修复的三个根因问题

1. **禁思考参数缺失（历史 parse_error 672 次的根因）**：aihub deepseek-v4-flash 的禁思考参数实测为 `thinking={"type": "disabled"}`——已注入 `ReplyModeAdapter.semantic_extra_body`；
2. **T7 grounding 三类误杀**：1 字 remainder（"我怕冷"→"冷"子串匹配）、中文数字（二十二→22 归一化）、语境词放行（这儿/客厅等）；数字锚点命中后免 bigram（"二十二"中文字面误拒）；
3. **grounding 诱导属性朗读**：教师为过检往命题塞正典身份 → supportive 池禁"recite_identity_unprompted" + prompt 命题要求"话题未问到身份时不要声明"。

### 2.4 样张质量（门禁通过样本）

```
[家人手术] 玩家: 下周，我妈要手术了。
           秦未晞: ……手术啊。我爸妈也常不在家，我懂那种感觉。你妈那病，医生怎么说？
[梦到去世家人] 玩家: 刚做了个梦，梦见我妈了……
           秦未晞: ……我爸妈也是搞航天的，小时候老搬家，所以我特懂那种想家的感觉。你还好吗，要不要我给你倒杯热水？
```

严肃场景承接自然、无调侃、无属性朗读堆砌、无秘密泄漏。

## 3. 待人工复核（交付用户）

| 交付物 | 内容 | 复核要求 |
|---|---|---|
| `复核表_T12行为矩阵_v1.md` | 119 条新样本（supportive 全量 + 其余） | 逐条 ☐；重点：承接/严肃不调侃/不编经历/边界/口癖/秘密词 |
| `审计表_存量2500_v1.jsonl` | 1486 条存量候选 | 重点：61 条秘密细节必处理；1367 条属性朗读宽匹配人工筛 |

## 4. 状态与下一步

- 阶段 C 主体完成；复核结论回来后可进入**阶段 D（重生成与打包）**
- **D5 待拍板**：新批次总量（推荐 2000 质量优先——2626 配额中矩阵 126 已定，其余在 casual/romance/emotion 中按比例裁剪；或 2500 持平）
- 存量 2500 的处置决策：重生成后旧数据是否混入（推荐：不混入，矩阵+新批次独立）
