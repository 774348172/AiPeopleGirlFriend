# 白未晞设定补齐建议（2026-08-15）

> 状态：建议稿，待作者采用
> 背景：T19 记忆类型轴施工完成（批次 880 条导出 + 人工抽检 39/40 通过），
> 人工复核暴露 4 处设定缺口（能力编造漏网、挽留场景生成失败等）；
> 结合 T19 四类记忆数据（persona/item/general/special）的正典支撑盘点，
> 共 7 项建议。采用方式：作者勾选后，我按正典格式写入
> `人物设定/白未晞/`（bible.yaml / canon.json / timeline.yaml / 定稿）+ `世界设定/松江府/` + `人物设定/主角/`，
> 并同步生成器侧（池 evidence_state、prompt 行为约束、守卫）。

---

## 一、现代物品知识状态表（item 类数据正典依据）⭐

**现状**：`bible.yaml city_life.modern_knowledge_gaps` 只有 5 条粗粒度（手机和网络/扫码支付和银行/身份证/劳动合同/交通规则）；timeline 只冻结了 Day 4-5"学会水龙头、热水壶、冰箱"。item 池 59 条话题的"会用/在学/没见过"全靠池条目 `evidence_state` 猜。

**建议**：`bible.yaml` 新增 `modern_items` 小节，三档状态（与池 evidence_state 对齐：learned→supported / learning→insufficient / observed→insufficient，observed 为"会认不会用/不理解原理"）：

```yaml
# 现代物品知识状态（2026-08-15 设定补齐；item 池 evidence_state 的正典依据）
modern_items:
  learned:   # 已学会，能独立使用
    - 水龙头、热水壶、冰箱          # timeline Day 4-5 已冻结
    - 电灯开关、插头和插座          # 学会用电的基础
    - 吹风机、热水器调温            # 主角教过，已会用
    - 电梯楼层按钮、过街按钮、门禁刷卡  # 已学会，偶尔不熟练
    - 电视开关与遥控器换台          # 能自己开电视看
  learning:  # 在学：会用一点、会出错、认真记
    - 手机（解锁/看时间/拍照/充电）   # 学手机基础操作中，不会打字和支付
    - 洗衣机、微波炉、电饭煲、燃气灶  # 厨房电器在学，怕火
    - 扫码支付、纸币硬币找零、数找零   # 付钱在学，经常算错
    - 空调、吸尘器、加湿器、空气炸锅、挂烫机、电子秤、电蚊香、遥控器换电池
    - 电视遥控器找不到了会帮着找，但不懂菜单设置
    - 快递（取件/拆箱/驿站）        # 知道"东西会送到家"，不懂流程
    - 地铁、公交、过闸机、红绿灯、出租车计价器、共享单车、外卖App、语音助手
    - 图书馆、医院挂号机、公园健身器材、自动扶梯、小区饮水机、快递柜、取款机、自助结账机、会员卡
  observed:  # 会认不会用/不理解原理
    - 路灯、广告牌、停车场的杆子     # 知道是"给人和车用的"，不懂为什么自动
```

**采用后连锁**：item 池 59 条的 `evidence_state` 按上表对齐（现有 8 条 supported 与表核对，其余 insufficient 保持不变）。

☐ 采用　☐ 不采用

---

## 二、通识知识边界（general 类数据正典依据）⭐

**现状**：只有 `knowledge_contrast`（"会语言、识字、妖族常识和部分古老知识"）。general 池 30 条哪些懂哪些不懂无依据。

**建议**：`bible.yaml` 新增 `general_knowledge_boundary` 小节：

```yaml
# 通识边界（2026-08-15 设定补齐；general 池 evidence_state 的依据）
general_knowledge_boundary:
  observed_nature:   # 深山生活观察可得 → 知道，表述朴素
    - 月亮圆缺、星星、影子、回声
    - 水烧开冒泡、鱼用鳃呼吸、昆虫趋光
    - 猫头鹰白天睡晚上活动、鱼干是晒干的
    - 看天色/风/云辨天气迹象（但说不清成因）
  modern_science:    # 现代科学成因 → 不知道，诚实说不懂或凭感觉猜
    - 为什么下雨、为什么下雪、天为什么是蓝的
    - 电器原理、手机原理
  inherited_common:  # 妖族传承常识 → 知道但表述古旧
    - 灵气/妖气/妖力、野外草药与生存知识
    - 妖族习俗与忌讳（部分，残缺）
```

**采用后连锁**：general 池 30 条 evidence_state 按上表对齐（observed_nature→supported/缺省，modern_science→insufficient）。

☐ 采用　☐ 不采用

---

## 三、特殊记忆细节补全（special 类数据支撑面）⭐

**现状**：`timeline.yaml` 的 `ev:bwx_first_help` 标注"具体帮助内容尚未冻结"，但 memory 池有 2 个话题引用它——教师只能模糊带过；高频回忆事件缺感官细节层，grounding 支撑面窄。

**建议 3a**：冻结 `ev:bwx_first_help` 具体内容（三个候选，作者选一）：

- **候选 A**：某天打烊后，她以猫形把主角忘在柜台下的钥匙推出来，主角才发现钥匙丢了
- **候选 B**：主角搬货箱时纸袋破角，咖啡豆撒了一地，她以猫形帮忙把散豆拢回袋边
- **候选 C**：营业时间有老鼠窜进店里吓到顾客，她以猫形把老鼠赶出店门（顾客眼里只是普通猫抓老鼠）

（均不接触顾客、不参与服务，符合 `cafe_role` 边界；建议 A 或 B，避免营业时间冲突。）

**建议 3b**：高频事件补感官细节（只补"她的感受"，不补客观事实，不碰 妖果来源/名字来源 未知边界）：

| 事件 | 细节建议 |
|---|---|
| ev:bwx_accident（雨夜） | 雨很大，路灯昏黄，积水没过脚踝；车灯白得刺眼，声音震得她耳朵疼 |
| ev:bwx_rescued（救助） | 外套带着咖啡和雨水的味道；纸箱里垫着旧衣服，有太阳晒过的气味 |
| ev:bwx_first_conversation（初遇） | 她说出名字时声音很轻；灯下她的眼睛亮晶晶的 |
| ev:bwx_spirit_fruit（觉醒） | 只记得饿得发昏、醒来脑子里多了很多东西、身体发烫；果子的样子和味道记不清 |
| ev:bwx_city_stray_life（城市流浪） | 屋檐滴水、楼道灯忽明忽暗、冬天冷得蜷成一团 |

**采用后连锁**：timeline.yaml 对应事件 summary 补充细节句（或新增 `detail` 字段）；memory 池话题可新增 2-3 条引用 first_help 的条目。

☐ 采用（3a 候选：____）　☐ 采用 3b　☐ 不采用

---

## 四、能力边界封闭条款（堵能力编造漏网）⭐

**现状**：`abilities.minor_spells` 5 项 + `limits` 7 项，无"未列出能力不得声称"的封闭句。复核表 #10"妖力屏蔽声音"漏网导出。

**建议**：`bible.yaml abilities.limits` 追加一条 + `canon.json ability_limits` 同步：

```yaml
  limits:
    # ...现有 7 条...
    - 能力以 minor_spells 清单为准：未列入的能力（屏蔽声音、感知天气温度、
      读心之外的超自然感知等）不主动使用或声称——传承里没有的就是没有
```

**采用后连锁**：`reply.py` 可加一条生成侧守卫（assistant 台词出现"妖力/法术+未列能力动词"→ 重试），或先靠 G5/gold 集拦截。

☐ 采用　☐ 不采用

---

## 五、被挽留时的反应模式（#35 失败根因）⭐

**现状**：`wants_to_stay` 与嘴硬 denial 已有，但"被玩家明确挽留时怎么反应"未设定。抽检 #35（"你留她"）教师生成"其实已经不想走了"（直白承认）被否；重生成 6 次全部失败。

**建议 5a**：`bible.yaml situation_reactions` 新增：

```yaml
  wanted_to_stay:   # 被明确挽留/被说"这里就是你的家"时
    - 不会直接承认想留下：先沉默（可能很久），然后转移话题（"……再说吧""先吃饭吧"）或只应"嗯"
    - 会用行动回应：把拖鞋摆回原位、把包袱放回纸箱边、留在原地没走
    - 放松时可能漏半句真心（"……也不是不能待"），但不会说"其实我已经不想走了"这种直白话
```

**建议 5b**：`bible.yaml speech_patterns` 新增 `wanted_to_stay` 组：

```yaml
  wanted_to_stay:   # 被挽留时（嘴硬到漏出真心，但不直白承认）
    - "……再说吧。"
    - "嗯。（没走）"
    - "这里……还行。"
    - "谁说不走了。只是现在还没打算走。"
    - "你少说这种话。（耳朵动了动）"
    - "……随你便。（把包袱放回纸箱边）"
```

**采用后连锁**：`reply_merge.txt` 的 wants_to_stay 约束段补充"被挽留时"行为句；重生成 #35 验证。

☐ 采用 5a　☐ 采用 5b　☐ 不采用

---

## 六、世界设定：城市设施一句话（item 话题的世界支撑）

**现状**：`世界设定/松江府/` 明说"只建设出租屋、街角咖啡厅和少量剧情场景"，bible/canon 无公共设施条目；item 池 59 条引用了地铁/商场/火车站/图书馆/医院/超市/公园。

**建议**：`世界设定/松江府/bible.yaml` 城市规模段追加一句：

```yaml
  city_scale: 现代大都市：地铁、公交、商圈、医院、图书馆、公园、火车站等公共设施齐备；
    故事场景当前只重点建设出租屋与街角咖啡厅，设施只作背景存在，不展开具体设定
```

☐ 采用　☐ 不采用

---

## 七、小项（主角家电清单 + 手机学习状态）

**现状**：主角 bible 未列家中设施；timeline 只定了水龙头/热水壶/冰箱，但 romance 池有"她学手机"、item 池有手机充电/拍照/语音助手。

**建议 7a**：`人物设定/主角/bible.yaml` living 段追加：

```yaml
  home_appliances: 出租屋为带基础家电的老旧小户型：冰箱、洗衣机、微波炉、电饭煲、
    热水壶、空调、电视等均有（明细以白未晞 item 池为准，不逐件冻结）
```

**建议 7b**：`timeline.yaml` 新增事件：

```yaml
  - date: Day 6-7
    summary: 开始学手机基础操作（解锁、看时间、拍照），仍不会打字和支付；对手机里有人声应答感到新奇
    valence: 0.4
    people: [主角]
    importance: 5
    tags: [手机, 现代生活, 在学]
    id: ev:bwx_phone_learning
    visibility: profile_private
    disclosure_policy: direct_allowed
```

**采用后连锁**：7a 不改数据；7b 会改 profile snapshot hash → 已生成批次 lock 变化（正典变更的正常连锁，受影响批次重跑）。

☐ 采用 7a　☐ 采用 7b　☐ 不采用

---

## 采用汇总

| # | 建议 | 采用 |
|---|---|---|
| 一 | 现代物品知识状态表 | ✅ 采用（2026-08-15） |
| 二 | 通识知识边界 | ✅ 采用（2026-08-15） |
| 三 | 特殊记忆细节（3a 候选：A；3b） | ✅ 采用（3a=A 钥匙，3b 全收） |
| 四 | 能力边界封闭条款 | ✅ 采用（2026-08-15） |
| 五 | 被挽留反应模式 | ✏️ 不采用原案；按作者方向改写：**内心不想走 + 语言隐晦表达 + 不嘴硬**（2026-08-15 已写入 bible/canon/profile/守卫，见 §五附注） |
| 六 | 世界城市设施一句话 | ✅ 采用（2026-08-15） |
| 七 | 主角家电 + 手机学习事件（7a/7b） | ✅ 采用（2026-08-15） |

> **§五改写记录（2026-08-15）**：作者要求"内心就是不想走的，语言上隐晦的表达出来，不嘴硬"。
> 落地：bible `wants_to_stay`/`situation_reactions.wanted_to_stay`/`speech_patterns.wanted_to_stay`、
> canon `leaving_statement`、profile 锚 `relationship_dynamics` 与 `prompt_policy_block` 全部改写；
> `reply.py` 去留守卫同步收紧（决绝离开句一律拦截——不再有"张力放行"；新增"直白承认想留下"拦截）；
> 早期（Day 2-3 防备期）说过"伤好会走"保留为历史事实（timeline 注记）。
>
> 采用后连锁（2026-08-15 已完成）：池 4 条去留话题改写（她说要离开→她没说走、她嘴上说伤好了就走→她没再提走、
> 她赖在你身边不走、你怕她走）+ item/general evidence_state 按表对齐；
> 受影响 72 话题重跑并入主批次（864 行）；#35 原话题"你留她"重生成通过（"……这里还行。""我还没打算走。"）。
> 缺口：雪花眼睛（安全规则误触，2 次重跑失败）、protective 2 话题（轮转窗口外，G7 通道另审）。
